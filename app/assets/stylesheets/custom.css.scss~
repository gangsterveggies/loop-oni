@import "bootstrap";

/* mixins, variables, etc. */

$ONIYellow: #FFE479;
$ONIYellowDark: #EDD161;
$SWhite: #F6F6F6;

@mixin background_mix {
  background-color: $SWhite;
  background-image: -moz-linear-gradient(top, $SWhite, White);
  background-image: -webkit-gradient(linear, 0 0, 0 100%, from($SWhite), to(White));
  background-image: -webkit-linear-gradient(top, $SWhite, White);
  background-image: -o-linear-gradient(top, $SWhite, White);
  background-image: linear-gradient(to bottom, $SWhite, White);
  background-repeat: repeat-x;
}

/* universal */

html {
  overflow-y: scroll;
}

body {
  padding-top: 160px;
}

section {
  overflow: auto;
}

textarea {
  resize: vertical;
}

.center {
  text-align: center;
  h1 {
    margin-bottom: 10px;
  }
}

/* typography */

h1, h2, h3, h4, h5, h6 {
  line-height: 1;
}

h1 {
  font-size: 3em;
  letter-spacing: -2px;
  margin-bottom: 30px;
  text-align: center;
}

h2 {
  font-size: 1.2em;
  letter-spacing: -1px;
  margin-bottom: 30px;
  text-align: center;
  font-weight: normal;
  color: $grayLight;
}

p {
  font-size: 1.1em;
  line-height: 1.7em;
}


/* header */

.navbar-inner {
  padding-top: 5px;
  padding-bottom: 5px;

  @include background_mix;
}

#logo {
  float: left;
  margin-right: 10px;
}
