Olá <%= @user.name %>,

Alguém, esperemos que tu, fez um pedido de reposição de password na tua conta. Para completar a reposição usa este link:

<%= make_password_reset_url(@user.password_reset_token) %>

Se não pediste para a tua password ser reposta ignora este e-mail e a tua password não mudará.

Cumprimentos,
A equipa do Loop